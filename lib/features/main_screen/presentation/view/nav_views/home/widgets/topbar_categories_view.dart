import 'package:flutter/material.dart';

class TopbarCategoriesView extends StatelessWidget {
  const TopbarCategoriesView({super.key});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Column(
        children: [
          Text('Topbar Categories View'),
        ],
      ),
    );
  }
}